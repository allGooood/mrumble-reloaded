export type CookieOption = {
    id: number,
    cookie_name: string,
    sku: string,
    stock: number,
    extra_charge: number,
    calories: string,
    image_url: string,
    category: string,
    description: string,
}